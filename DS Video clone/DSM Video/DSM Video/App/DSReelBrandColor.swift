import SwiftUI

enum DSReelBrandColor {
  static let background = Color.dsBackground  // single source of truth — see DSReelDesignTokens.swift
  static let accent = Color.dsAccent          // explicit alias for design token migration
}

