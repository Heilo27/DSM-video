import SwiftUI

enum DSReelBrandColor {
  static let background = Color.dsAccent  // single source of truth — see DSReelDesignTokens.swift
  static let accent = background  // alias for design token migration
}

