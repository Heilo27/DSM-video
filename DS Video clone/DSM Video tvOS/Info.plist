<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleDisplayName</key>
	<string>DSM Video</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIcons</key>
	<dict>
		<key>CFBundlePrimaryIcon</key>
		<string>AppIconTV</string>
	</dict>
	<key>CFBundleIcons~tv</key>
	<dict>
		<key>CFBundlePrimaryIcon</key>
		<string>AppIconTV</string>
	</dict>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>$(PRODUCT_BUNDLE_PACKAGE_TYPE)</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleTypeRole</key>
			<string>Editor</string>
			<key>CFBundleURLName</key>
			<string>HeiloProjects.DSReel.deeplink</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>dsvideo</string>
			</array>
		</dict>
	</array>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSApplicationCategoryType</key>
	<string>public.app-category.entertainment</string>
	<key>MinimumOSVersion</key>
	<string>17.0</string>
	<!-- ATS: cleartext is scoped to the local network ONLY (TASK-777).
	     NSAllowsLocalNetworking permits http:// to LAN / .local / RFC1918 hosts — the LAN
	     login candidates — and nothing public.

	     REMOTE ACCESS DEPENDS ON THIS BEING UNDERSTOOD BEFORE IT IS CHANGED.

	     Removing NSAllowsArbitraryLoads in aae7d66 (2026-04-10) silently broke every remote
	     login, because the only remote path in use was http:// to a raw WAN IP. Nothing
	     tested that path, so it went unnoticed for months and then cost an entire evening to
	     rediscover — the app reported "check that DSVideoServer is running" for a server that
	     was answering fine.

	     The supported remote path is HTTPS to a DDNS hostname with a valid certificate
	     (e.g. https://<name>.synology.me:5001). That satisfies ATS with NO exception at all,
	     keeps credentials encrypted, and needs no App Review justification.

	     Do NOT add NSAllowsArbitraryLoads to "fix" a remote-access report. It re-enables
	     plaintext passwords over the internet. Raw-IP remote access is unsupported by design:
	     a certificate cannot vouch for an IP address, so https:// to an IP fails validation
	     and http:// to a public IP is blocked here. Point the user at their DDNS hostname. -->
	<key>NSAppTransportSecurity</key>
	<dict>
		<key>NSAllowsLocalNetworking</key>
		<true/>
	</dict>
	<key>NSBonjourServices</key>
	<array>
		<string>_dsvideo._tcp</string>
	</array>
	<key>NSLocalNetworkUsageDescription</key>
	<string>DSM Video connects to your Synology NAS on the local network to stream your video library.</string>
	<key>UIRequiredDeviceCapabilities</key>
	<array>
		<string>arm64</string>
	</array>
</dict>
</plist>
